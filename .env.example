# ShowCV Node.js 服务环境变量
# 复制为 .env 并填入真实值

# 服务端口
PORT=3070

# 数据存储（SQLite 本地数据库）
# 默认 data/showcv.db，可通过此变量覆盖路径
DB_PATH=data/showcv.db

# AES-256-GCM 加密密钥（32 字节 hex）
# 生成命令：openssl rand -hex 32
ENCRYPTION_KEY=

# AI 优化功能（可选，留空则禁用）
AI_API_KEY=
AI_BASE_URL=
AI_MODEL=
AI_THINKING=
